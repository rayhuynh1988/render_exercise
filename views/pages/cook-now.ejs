<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cook Now</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css"/>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/navbar'); %>

<main class="container">
    <h2>Your Dinner Plan</h2>

    <p><strong>Suggested Dinner Time: </strong><%= avgDinnerTime %></p>
    <p><strong>Suggested Cuisine: </strong><%= cuisine %></p>

    <h3>Top Restaurants for <%= cuisine %>:</h3>
    <% if (restaurants && restaurants.length > 0) { %>
        <ul>
            <% restaurants.forEach(restaurant => { %>
                <li>
                    <a href="<%= restaurant.link %>" target="_blank"><%= restaurant.title %></a>
                    <p><%= restaurant.snippet %></p>
                </li>
            <% }); %>
        </ul>
    <% } else { %>
        <p>No restaurants found for this cuisine.</p>
    <% } %>
</main>
</body>
</html>
