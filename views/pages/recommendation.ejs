<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recommendations</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css"/>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <main class="container">
        <%- include('../partials/navbar'); %>

        <h1>Hello, <%= name %>!</h1>
        <p>Here are your dinner recommendations based on your preferences:</p>
        <p><strong>Cuisine:</strong> <%= cuisine %></p>
        <p><strong>Price Range:</strong> <%= price %></p>
        <p><strong>Food Composition:</strong> <%= food_composition %></p>

        <% if (recommendations && recommendations.length > 0) { %>
            <ul>
                <% recommendations.forEach((restaurant) => { %>
                    <li>
                        <strong><%= restaurant.title %></strong>
                        <p><strong>Cuisine:</strong> <%= restaurant.cuisine %></p>
                        <p><strong>Price:</strong> <%= restaurant.price %></p>
                        <p><strong>Location:</strong> <%= restaurant.location %></p>
                        <p><strong>Food Composition:</strong> <%= restaurant.composition %></p>
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <p>No recommendations found for your preferences. Try changing your filters!</p>
        <% } %>
    </main>
</body>
</html>
